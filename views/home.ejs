<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <nav class="flex items-center justify-between flex-wrap bg-blue-400 p-6">
        <div class="flex items-center flex-shrink-0 text-white mr-6 mr-80">
          
          <span class="font-semibold text-xl tracking-tight">WEB TEST 3</span>
        </div>
        <div class="block lg:hidden">
          <button class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
          </button>
        </div>
        <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
          <div>
            <a href="/phones/create" class="inline-block ml-80 text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white  lg:mt-0 mr-20">Add Phone</a>
          </div>
          <div>
            <a href="/api/auth/logout" class="inline-block ml-80 text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0 mr-20">Logout</a>
          </div>
          <div>
            <h1>Welcome <%= user.username %>!</h1>
            <p>Email: <%= user.email %></p>
          </div>
        </div>
      </nav>

     
    <h2 >All Phones:</h2>
    <ul>
        <% allPhones.forEach(user => { %>
            <li><%= user.phonename %> - <%= user.price %> - <%= user.isIos %></li>
            <div>
              <a href="/phones/delete/<%= user.id %>" data-method="DELETE" class="inline-block ml-80 text-sm px-4 py-2 leading-none border rounded text-black border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0 mr-20">Delete</a>

            <div>
              <a href="/phones/<%= user.id %>" data-method="Get" class="inline-block ml-80 text-sm px-4 py-2 leading-none border rounded text-black border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0 mr-20">show</a>
            </div>
        <% }); %>
    </ul>
</body>
</html>